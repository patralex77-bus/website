{% extends "admin/base_admin.html" %}
{% block content %}
<div class="mb-8 flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between">
  <div>
    <a href="{{ url_for('admin.bus_request_detail', item_id=item.id) }}" class="font-bold text-red-700">← Zurück zur Anfrage</a>
    <h1 class="mt-3 text-4xl font-black">Angebotsentwurf #{{ item.id }}</h1>
    <p class="mt-2 text-slate-600">Vorlage für E-Mail oder Angebotstext. Wird nicht automatisch versendet.</p>
  </div>
  <div class="flex flex-wrap gap-2">
    <a href="mailto:{{ item.email }}?subject={{ offer.mailto_subject }}&body={{ offer.mailto_body }}" class="rounded-2xl bg-red-700 px-5 py-3 font-black text-white hover:bg-red-800">E-Mail öffnen</a>
    <button type="button" onclick="copyOffer()" class="rounded-2xl bg-slate-900 px-5 py-3 font-black text-white hover:bg-slate-800">Text kopieren</button>
    <form method="post" action="{{ url_for('admin.bus_request_mark_offer_sent', item_id=item.id) }}">
      <input type="hidden" name="_csrf_token" value="{{ csrf_token() }}">
      <button class="rounded-2xl border border-slate-200 bg-white px-5 py-3 font-black hover:bg-slate-50">Als gesendet markieren</button>
    </form>
  </div>
</div>

{% if not calculation %}
  <div class="mb-6 rounded-2xl border border-amber-200 bg-amber-50 p-5 font-bold text-amber-800">
    Für diese Anfrage gibt es noch keine Pricing-Engine-Kalkulation. Der Angebotsentwurf enthält daher noch keinen finalen Preis.
  </div>
{% endif %}

<div class="grid gap-8 xl:grid-cols-[.9fr_1.1fr]">
  <section class="rounded-[2rem] bg-white p-8 shadow-sm">
    <h2 class="text-2xl font-black">Angebotsdaten</h2>

    <div class="mt-6 grid gap-4">
      <div class="rounded-2xl bg-slate-50 p-4">
        <div class="text-xs font-black uppercase tracking-[.16em] text-slate-500">Empfänger</div>
        <div class="mt-2 font-bold">{{ item.contact_name }}</div>
        <div class="text-sm text-slate-600">{{ item.organisation or '-' }}</div>
        <div class="text-sm text-slate-600">{{ item.email }}{% if item.phone %} · {{ item.phone }}{% endif %}</div>
      </div>

      <div class="rounded-2xl bg-slate-50 p-4">
        <div class="text-xs font-black uppercase tracking-[.16em] text-slate-500">Route</div>
        <div class="mt-2 font-bold">{{ offer.route_line }}</div>
      </div>

      <div class="grid gap-4 sm:grid-cols-2">
        <div class="rounded-2xl bg-slate-50 p-4">
          <div class="text-xs font-black uppercase tracking-[.16em] text-slate-500">Datum</div>
          <div class="mt-2 font-bold">{{ offer.date_line }}</div>
        </div>
        <div class="rounded-2xl bg-slate-50 p-4">
          <div class="text-xs font-black uppercase tracking-[.16em] text-slate-500">Zeit</div>
          <div class="mt-2 font-bold">{{ offer.time_line }}</div>
        </div>
      </div>

      <div class="grid gap-4 sm:grid-cols-2">
        <div class="rounded-2xl bg-slate-50 p-4">
          <div class="text-xs font-black uppercase tracking-[.16em] text-slate-500">Gruppe</div>
          <div class="mt-2 font-bold">{{ offer.pax_line }}</div>
        </div>
        <div class="rounded-2xl bg-slate-50 p-4">
          <div class="text-xs font-black uppercase tracking-[.16em] text-slate-500">Bus</div>
          <div class="mt-2 font-bold">{{ offer.bus_line }}</div>
        </div>
      </div>

      {% if calculation %}
        <div class="rounded-3xl bg-slate-900 p-6 text-white">
          <div class="text-sm font-black uppercase tracking-[.18em] text-red-300">Preis</div>
          <div class="mt-3 text-4xl font-black">{{ calculation.gross_total }} €</div>
          <div class="mt-1 text-sm text-slate-300">Brutto · Netto {{ calculation.net_total }} € · USt {{ calculation.vat_amount }} €</div>
          <div class="mt-3 text-sm text-slate-300">{{ calculation.profile.name }} · {{ calculation.total_km }} km · {{ calculation.operating_hours }} h</div>
        </div>
      {% endif %}
    </div>
  </section>

  <section class="rounded-[2rem] bg-white p-8 shadow-sm">
    <h2 class="text-2xl font-black">E-Mail Text</h2>
    <p class="mt-2 text-slate-600">Diesen Text kannst du kopieren oder über „E-Mail öffnen“ direkt in deinem Mailprogramm verwenden.</p>

    <textarea id="offerText" class="mt-6 min-h-[620px] w-full rounded-2xl border border-slate-200 bg-slate-50 p-5 font-mono text-sm leading-7 outline-none focus:border-red-700">{{ offer.plain_text }}</textarea>

    <div class="mt-5 flex flex-wrap gap-3">
      <button type="button" onclick="copyOffer()" class="rounded-2xl bg-slate-900 px-5 py-3 font-black text-white hover:bg-red-700">Text kopieren</button>
      <a href="mailto:{{ item.email }}?subject={{ offer.mailto_subject }}&body={{ offer.mailto_body }}" class="rounded-2xl bg-red-700 px-5 py-3 font-black text-white hover:bg-red-800">E-Mail öffnen</a>
    </div>
  </section>
</div>

<script>
function copyOffer() {
  const textarea = document.getElementById('offerText');
  textarea.select();
  textarea.setSelectionRange(0, 99999);
  navigator.clipboard.writeText(textarea.value).then(() => {
    alert('Angebotstext wurde kopiert.');
  }).catch(() => {
    document.execCommand('copy');
    alert('Angebotstext wurde kopiert.');
  });
}
</script>
{% endblock %}
